name: Filesystem Sandboxing MCP server
version: 0.0.1
schema: v1
mcpServers:
  - name: filesystem
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      - "D:/document/Unity_Projects/Thronefall/"